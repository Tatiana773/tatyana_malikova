
let numA = 1;
let numB = 8;
let numMin;
let numMax;
if(numA < numB){
    numMin = numA;
    numMax = numB;
}
else if(numA > numB){
    numMin = numB;
    numMax = numA;
}
console.log(numMax);
console.log(numMin);